[build]
dockerfilePath = "control-plane/Dockerfile"
watchPatterns = ["control-plane/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
